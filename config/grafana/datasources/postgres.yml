apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    user: postgres
    secureJsonData:
      password: postgres_password
    jsonData:
      database: velov_db
      sslmode: disable
      postgresVersion: 1500
      timescaledb: false
    isDefault: true